# Premiery App Backend

It's a simple backend app that stores movies and it's data in SQLite database and allows them to be fetched by the [Premiery app](https://github.com/jegor377/premiery) via REST API written in Flask.
